import { paginationMarkup } from './pagination.js';

export function initExercises() {
  // TODO: Implement filters, server search on submit, category/exercise cards.
  // Use paginationMarkup(currentPage, totalPages) to render the agreed controls.
  // Insert the markup into [data-pagination] and handle clicks through data-page.
  void paginationMarkup;
}
